- page_title!('Editando service')

#region-services.container

  .region
    .region-title.page-header
      %h1
        Editando
        %b
          service

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @service, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
